# 컨텍스트 노트 — 크롬 팻 확장 하네스

작업 중 내린 결정과 근거를 계속 덧붙인다.

## 2026-07-22 초기 결정

### 결정 1 — 구축 순서: 검증 하네스 먼저
CLAUDE.md는 "명령어 한 줄로 검증 가능"을 전제하지만 package.json조차 없었다.
에이전트 팀을 아무리 잘 만들어도 `npm run check`가 없으면 루프가 성립하지 않는다.
→ 검증 하네스(도구 체계)를 먼저 부트스트랩한 뒤, 그 위에 에이전트 팀을 얹는다.

### 결정 2 — 실행 모드: 전문가 3~4인 팀 (생성-검증 지향)
core-logic-dev(순수함수 TDD) / chrome-adapter-dev(크롬 API+background+content) /
harness-qa(검증 게이트·E2E·콘솔 에러 0). sidepanel UI는 초기엔 adapter에 흡수, 필요 시 분리.

### 결정 3 — git: 홈 디렉토리 전체가 단일 저장소
chrome-pet은 독립 repo가 아니라 C:\Users\PC-727 홈 repo에 중첩돼 있고,
web-mcp-provider 등 무관한 미커밋 변경이 많다. 함부로 브랜치/커밋하면 얽힌다.
→ 커밋은 사용자 요청 시에만, chrome-pet 경로만 스코프 스테이징. 자동 커밋 금지.

### 결정 4 — `npm run check`에서 E2E 분리 (CLAUDE.md 스펙 대비 의도적 편차)
CLAUDE.md 1.1은 check가 e2e까지 전부 포함한다고 명시.
그러나 MV3 확장 E2E는 headed 크롬 + dist 빌드가 필요해 루프 게이트로는 느리고 flaky하다.
→ `check` = lint→typecheck→test→validate→build (빠른 게이트).
   `test:e2e`는 별도 명령으로 분리. 이유와 수동 실행법을 여기 남긴다.
   수동 E2E: `npm run build && npm run test:e2e` (headed 크롬 필요).
   추후 CI에서 xvfb로 check:full에 통합 가능.

### 결정 6 — feed E2E 레이스: 기능 아닌 테스트 타이밍 문제로 처리
feed.spec.ts 최초 실행 실패 — 주입한 hunger 50 대신 기본값(0/100) 렌더.
원인: background onInstalled 의 `if(!existing) savePet(createPet)` 비동기 초기화가
테스트의 상태 주입과 레이스. 뒤늦은 기본값 write 가 주입값을 덮어씀.
실사용엔 동시 writer 없어 무해 → 기능 코드는 안 건드리고 테스트만 수정.
수정: 주입 전 storage 에 `pet` 키 생길 때까지 expect.poll 대기(초기화 착지 보장) +
주입 후 재확인 방어 assertion. 향후 반복 시 waitForPetInitialized(sw) 헬퍼 추출 후보.

### 결정 7 — 데스크톱 팻(A+B) 완료, 보류된 UX 경계 2건
루프 A(오버레이·중력·배회·상태 표정) + 루프 B(요소 안착/낙하) 구현 완료.
petBehavior 순수 물리(step/spriteFrame), content 오버레이 rAF + 요소 타깃팅.
harness-qa가 지적한 UX 경계 2건 — 계약 위반 아님, 제품 결정 대기:
 A. perched 중 위로 스크롤해 요소 top이 얕게 걸치면(요소는 화면에 크게 보임) 팻만 조기 낙하.
    isValidRect의 top<0 / top>innerHeight-48 클램프 비대칭 때문.
 B. 타깃 해제 후 재타깃 4초 쿨다운 → 낙하 시작하면 바닥까지 떨어진 뒤에야 재안착(인접 요소 즉시 재안착 불가).
둘 다 고치려면 content 휴리스틱 수정 필요. 필요 시 별도 루프.

### 결정 8 — 실제 스프라이트 도입: 컷아웃→시트 파이프라인, 크기 64×104
사용자가 배경 제거 투명 PNG(433×577) 제공.
`scripts/make-sprite.mjs`가 알파 트림→면적평균 다운샘플→바닥 정렬로 6프레임 시트(src/assets/pet.png) 생성.
포즈 1개뿐이라 6프레임 동일(정적). build는 gen-sprite 생성 대신 src/assets/pet.png 복사로 전환, gen-sprite.mjs 삭제.
- 크기 48→64×104로 상향(SPRITE_W/H). 48px에선 디테일 뭉개져 식별 불가였음. core 상수·make-sprite·E2E 스펙 픽셀값 동반 수정.
- **버그 교훈:** 다운샘플 sx1 계산에서 box.x를 이중 가산해 픽셀당 소스 51px를 평균→극심한 블러. 출력 이미지를 Read로 직접 보고 발견(추측 아님). 범위식 수정 후 선명.
- 진짜 걷기/낙하 애니메이션은 포즈별 컷아웃을 더 받아야 가능(현재 정적).

### 결정 9 — 6포즈 시트로 애니메이션화 (결정 8의 정적 한계 해소)
사용자가 포즈별 6프레임 시트(sprite_images.png, 1629x965, RGB 배경 있음) 제공.
make-sprite.mjs 재작성: 테두리 flood-fill로 근백색 배경 투명화(내부 흰색 보존) →
열별 불투명 픽셀로 6프레임 자동 분리(갭 기준) → 각 트림·축소해 64x104 시트 합성.
이제 walk1/walk2 번갈이 등 진짜 애니메이션. 배경 임계 BG_MIN=236, 6구간 미검출 시 균등분할 폴백.
코드(core/content/E2E)는 무변경 — 64x104 유지, make-sprite만 교체.

### 결정 5 — core/의 chrome.* 금지를 eslint로 강제
full 커스텀 룰 대신 flat config override에서 no-restricted-globals로 src/core/** 에서
`chrome` 전역 사용을 에러 처리. 가볍고 실효성 있음.

### 결정 10 — 낮잠(sleeping)은 연출 전용, 상태 보상 제거
증상 보고: "주말이 지났는데 팻이 배고픔을 안 느낀다."

**측정으로 확인한 원인** (`step()` 1시간 시뮬레이션)
- `petBehavior` super-cycle = 4×(WALK_MS 5000 + IDLE_MS 900) + WALK_MS 5000 + SLEEP_MS 10000 = **38.6초**
- content 렌더 루프가 `sleeping → 비sleeping` 전이마다 `rest()`(배고픔 −20)를 storage 에 적용
- 1시간당 깸 **93회** → **−1860/시간** vs `decay` **+10/시간** → 순 **−1850/시간** (회복이 감쇠보다 186배 빠름)
- 결과: 48시간 방치로 hunger 100 이 되어도, 페이지에 팻이 뜬 뒤 깸 5회(**약 3.2분**)면 0 으로 리셋. 행복도도 같은 구조로 100 고정.
- `decay` 자체는 정상이었다. 설정값 오타가 아니라 **애니메이션 주기에 상태 보상을 묶은 설계 충돌**이었다.

**수정:** content 의 `rest()` 적용 블록 제거 + 유일한 호출자가 사라진 `core/petState.rest()` 제거.
배고픔·행복도는 시간 경과(`decay`)와 사용자 행동(`feed`)으로만 변한다. 낮잠은 sleep 프레임 연출로만 남는다.

**하네스 보강:** `tests/e2e/sleep-no-recovery.spec.ts` 추가.
한 super-cycle(약 43초) 관측하며 (1) storage.pet.hunger 가 주입값 미만으로 내려가지 않는지,
(2) 실제로 sleep 프레임(backgroundPositionX −448px)이 관측됐는지 함께 검증한다.
(2)가 없으면 낮잠을 지나치지 않은 것이라 통과가 위장되므로 명시적으로 실패시킨다.
버그 상태로 되돌려 실패(hunger 50 → 30)를 먼저 확인한 뒤 수정 후 통과를 확인했다.
**교훈:** 결정 9 시점에 이 낮잠 보상은 E2E 커버리지가 없었다. 상태를 바꾸는 기능은 반드시 하네스 시나리오를 동반한다.

## 2026-08-06 문서 동기화

### 결정 11 — E2E 를 `check` 게이트에 편입 (결정 4 번복)

커밋 `460fcae` 로 이미 반영됐으나 근거가 여기 기록되지 않아 뒤늦게 남긴다.
결정 4 는 E2E 가 느리고 flaky 하다는 이유로 `check` 에서 분리했다. 그 대가가 드러났다.
낮잠 회복 버그(결정 10)와 컨텍스트 무효화 버그는 **유닛 테스트가 전부 그린인 상태에서 커밋됐다**.
둘 다 크롬 런타임에서만 드러나는 배선 문제라 유닛 게이트로는 원리적으로 못 잡는다.
→ `check` = lint→typecheck→test→validate→build→**test:e2e**. 게이트가 약 3분(E2E 2.4분)으로 느려지지만,
   "유닛 그린 + 확장 깨짐" 상태가 커밋되는 것을 막는 편이 싸다.
- flaky 우려는 결정 4 시점의 추정이었다. `page.route` 로 하네스를 복구한 뒤(`9534b01`) 실측상 안정적이다.
- 여전히 headed 크롬이 필요하다. CI 도입 시 xvfb 가 전제다.

### 결정 12 — 결정 3 은 폐기 (git 저장소 구조 정정)

결정 3 은 "chrome-pet 이 홈 디렉토리 단일 repo 에 중첩돼 있다"고 기록했으나 현재는 사실이 아니다.
chrome-pet 은 자체 저장소이고 remote 는 github.com/jjang750/chrome-pet.git, 기본 브랜치는 main 이다.
→ pathspec 스코프 스테이징은 이제 필수가 아니다. 다만 무관한 미커밋 산출물이 쌓이므로
   커밋 전 `git status` 확인 습관은 유지한다.

### 결정 13 — perch 판정을 "선정"과 "체류" 로 분리 (결정 7-A 해소)

결정 7 이 보류한 경계 A. perched 중 스크롤하면 요소가 화면에 크게 보이는데도 팻만 조기 낙하했다.

**원인**: `isValidRect` 하나를 후보 선정(`pickTarget`)과 체류 유지(`computePerch`) 양쪽에 겸용했다.
선정 기준은 "올라앉은 팻이 온전히 보여야" 하므로 `top <= innerHeight - SPRITE_H`(104px 여유)가 맞다.
그 기준을 체류에도 적용하면, 스크롤로 요소 top 이 104px 밴드 안으로 들어오는 순간 타깃이 해제된다.
상단은 여유 0(`top < 0`), 하단은 여유 104 — 결정 7 이 지적한 "클램프 비대칭" 이 이 겸용에서 나왔다.

**수정**: 공통 조건(`isUsableRect`: 크기·좌우·가시성)을 뽑고 세로 기준만 둘로 나눴다.
- 선정 `isValidRect` — `0 <= top <= innerHeight - SPRITE_H` (그대로 엄격)
- 체류 `canKeepPerch` — `KEEP_MARGIN(16) <= top <= innerHeight - KEEP_MARGIN`
즉 발끝이 화면에 남아 있는 동안은 유지하고, 새로 고를 때만 팻이 온전히 보이는 요소를 고른다.

**하네스**: `tests/e2e/perch-scroll.spec.ts` 추가. 요소 top 을 "선정 기준 밖·체류 기준 안" 밴드(`vh-60`)로
스크롤해 옮긴 뒤, 팻이 ground(`vh-104`)가 아니라 새 perch y(`vh-164`)를 1초간 추종하는지 표본 5개로 확인한다.
수정 전 실패(바닥 낙하) → 수정 후 통과를 먼저 확인했다.
- 밴드 판별이 성립하도록 두 y 가 20px 이상 벌어졌는지도 테스트가 직접 검사한다(뷰포트가 작아도 위장 통과 방지).

### 결정 14 — 재타깃 쿨다운을 벽시계에서 "지면 도착" 으로 교체 (결정 7-B 부분 해소)

결정 7 이 보류한 경계 B. 하차 후 재안착이 굼떴다.

**원인**: 하차 시 `nextRetargetAt = now + RETARGET_INTERVAL(4000)` 로 벽시계 쿨다운을 걸었다.
낙하는 0.6~1.3초면 끝나므로, 착지 후에도 남은 2~3초를 아무 목적 없이 배회했다.
쿨다운의 원래 목적은 두 가지였는데 둘 다 다른 장치로 이미 충족된다.
- "방금 떠난 요소로 곧바로 다시 올라가는 것" 방지 → `lastLeftEl` 제외가 담당한다.
- "공중에서 타깃을 잡아 순간이동하는 것" 방지 → 아래 게이트가 더 정확하게 담당한다.

**수정**: 하차 시 벽시계 쿨다운을 제거하고, `computePerch` 의 재탐색을 `onGround` 게이트로 감쌌다.
`body.pos.y >= innerHeight - SPRITE_H - 0.5` 일 때만 새 타깃을 고른다.
`RETARGET_INTERVAL` 은 후보가 없을 때 `*` 전체 스캔을 throttle 하는 원래 역할로만 남긴다.
- **부수 효과(잠재 버그 제거)**: 공중·드래그 중에 타깃이 잡히면 core 의 미정렬 분기가 `y: ground` 를
  반환해 팻이 지면으로 순간이동한다. 기존엔 쿨다운이 확률을 낮췄을 뿐 막지는 못했다(4초 넘게 드래그하면 발생).
  게이트는 이 경로를 원천 차단한다.

**하네스**: `tests/e2e/perch-retarget.spec.ts` 추가. 화면 하단 근처(top≈vh-140)에 후보를 하나 두어
낙하·상승 거리를 140px 로 줄이고, 하차→재안착 실측 시간이 3500ms 이하인지 본다.
수정 전 6689ms 실패 → 수정 후 통과. 상한만 보면 하차 감지 위장으로 통과할 수 있어
낙하 시간의 절반 이상이라는 하한도 함께 검사하고, 예산이 물리적으로 가능한지도 테스트가 직접 계산한다.

**남는 부분**: "공중에서 인접 요소로 곧바로 건너가기" 는 여전히 안 된다. 팻은 지면까지 내려온 뒤 다음 요소로 걸어간다.
요소 간 도약은 core 에 점프 물리가 없어 새 행동을 추가해야 하므로 이 루프의 범위에서 제외했다.

### 결정 15 — 대화 기반 성장 (결정 10 의 불변식 확장), 사후 기록

커밋 `7419696` 에서 들어간 기능인데 당시 결정 노트가 남지 않았다. 2026-08-06 문서 동기화 때 사후 기록한다.

**내용**: `core/petState.ts` 에 `chat(state, message) → {state, reply}` 와 `levelFromXp(xp)` 를 추가했다.
대화 1회당 XP +15 · 친밀도 +6 · 행복 +8 · 배고픔 +2, 50 XP 마다 레벨 +1. 답변은 배고픔·행복·키워드로
분기하는 고정 문구다 — **LLM 호출이 없다.** 순수 함수 제약(시간·랜덤 주입 금지)을 지키기 위한 선택이며,
`lastUpdated` 를 건드리지 않아 감쇠 타이머와 독립적이다. 사이드패널은 저장값을 그대로 읽지 않고
`chat(decay(current, now), message)` 로 **감쇠를 먼저 반영한 뒤** 성장시킨다(storage 단일 진실 유지).

**불변식 변경**: 결정 10 이 세운 "배고픔·행복을 바꾸는 경로는 decay 와 feed 둘뿐" 이 이제 셋이 된다.
`chat` 이 셋째다. 낮잠처럼 연출이 몰래 상태를 만지는 것과 달리 사용자의 명시적 입력에 대한 보상이므로
결정 10 의 취지(연출은 상태를 바꾸지 않는다)와는 충돌하지 않는다. 다만 상태를 바꾸므로 규칙대로
`tests/e2e/chat.spec.ts` 를 붙였다(친밀도·XP·레벨·대화 횟수가 오르고 storage 에 저장되는지 확인).

**남는 부분 2가지**
- `chattedAt` 을 storage 에 쓰지만 읽는 곳이 없다. `fedAt` 은 content 가 읽어 eat 애니메이션을 트는데,
  대화 전용 연출은 아직 없다. 훅만 남긴 상태 — 연출을 붙이거나 키를 지우거나 둘 중 하나로 정리해야 한다.
- 레벨·친밀도가 사이드패널 숫자로만 존재하고 팻의 외형·행동에 반영되지 않는다.

### 결정 16 — 대화 기반 성장 롤백 (결정 15 철회)

사용자 판단으로 **레벨·친밀도 같은 성장 기능은 이 팻에 필요 없다**고 결론냈다.
결정 15 에서 사후 기록했던 기능을 그대로 되돌린다.

**방식**: `git reset` 이 아니라 `git revert 7419696` 을 썼다. 두 가지 이유다.
- 성장 커밋 뒤에 perch 버그 수정 2건(`3938708` 조기 낙하, `b6505a3` 재타깃 게이트)과 워크스페이스 정리(`aeeb028`)가
  쌓여 있다. 성장 기능과 무관한 수정이라 reset 하면 같이 날아간다.
- `7419696` 은 이미 `origin/main` 에 push 돼 있어 reset 은 force-push 를 요구한다. revert 는 이력을 보존한다.

되돌린 커밋이 건드린 파일은 5개(`petState.ts`·`petState.test.ts`·`sidepanel/index.ts`·`sidepanel.html`·`tests/e2e/chat.spec.ts`)뿐이고,
이후 커밋들이 이 파일들을 손대지 않아 **충돌 없이 revert 됐다.** `bond|xp|level|chatCount|chattedAt` 전수 검색으로 잔여 참조 0건을 확인했다.

**불변식 복구**: 결정 15 가 셋으로 늘렸던 "배고픔·행복을 바꾸는 경로" 가 다시 **decay(시간)·feed(먹이) 둘**로 돌아왔다.
결정 10 이 세운 원래 불변식 그대로다.

**부수 정리**: `chattedAt` storage 키도 함께 사라졌다. 결정 15 가 "소비처 없는 훅" 으로 남겨둔 항목이었는데
롤백으로 자연히 해소됐다 — content 는 여전히 `fedAt` 만 읽는다.

**게이트**: `npm run check` 그린. 유닛 90→85(chat/levelFromXp 테스트 5개 제거), E2E 13→12(`chat.spec.ts` 제거).

**되살리는 법**: 성장 기능이 다시 필요해지면 이 revert 커밋을 되돌리면 된다(`git revert <이 커밋>`).
원본 구현은 `7419696` 에 그대로 남아 있다.

### 결정 17 — 캐릭터 선택 기능

사용자가 두 번째 스프라이트(`pet2.png`, 하늘색 코끼리 후드)를 추가하며 캐릭터 선택을 요청했다.

**아트 처리**: 사용자가 `dist/` 에 넣었는데 `build.mjs` 가 시작할 때 `rm -rf dist` 를 하므로 다음 빌드에서 사라진다.
`src/assets/` 로 옮겨 소스로 편입했다. 규격은 576×104 로 기존과 같지만 **그려진 캐릭터가 절반 크기**(높이 30~48px vs 83~100px)이고
바닥이 2px 떠 있었다. 크기는 사용자 판단으로 **그대로 두고**(작은 팻), 바닥 2px 만 아래로 시프트해 정렬을 맞췄다.
`make-sprite.mjs` 로 키우는 선택지는 이미 축소된 시트를 2.2배 업스케일하는 것이라 화질이 뭉개져 제외했다 —
고해상도 원본 아트가 생기면 그때 재생성한다.

**설계**: 캐릭터는 팻의 생체 상태가 아니라 **표시 설정**이므로 `PetState` 에 넣지 않고
`chrome.storage.local` 의 별도 키 `character` 에 id 만 저장한다. `fedAt` 과 같은 결이다.
결정 10 의 불변식("배고픔·행복을 바꾸는 경로는 decay·feed 둘뿐")은 그대로 유지된다 — 캐릭터 선택은 수치를 건드리지 않는다.

**core**: `src/core/characters.ts` 에 `CHARACTERS` 목록과 `resolveCharacter(id)` 를 뒀다.
모르는 id·빈 문자열·비문자열(숫자·객체)은 전부 기본 캐릭터로 떨어진다 —
storage 가 오염되거나 캐릭터가 목록에서 빠졌을 때 **팻이 통째로 안 보이는 사고**를 막는 안전장치다.
프레임 순서·셀 규격(64×104 9프레임)은 캐릭터 공통이라 시트만 갈아끼우면 되고 위치 계산은 손대지 않는다.

**교체 방식**: content 는 `storage.onChanged` 에서 `el.style.backgroundImage` 만 바꾼다.
rAF 루프 밖이라 백그라운드 탭에서도 즉시 반영되고, 팻의 위치·모드·물리 상태는 그대로 이어진다(리로드 불필요).

**하네스 보강**: 캐릭터를 추가하려면 `characters.ts`·`manifest.web_accessible_resources`·`src/assets/` **세 곳**이 일치해야 하고,
하나라도 빠지면 그 캐릭터를 고른 순간 404 로 팻이 조용히 사라진다. 타입 검사로는 안 잡히므로
`validate-manifest.mjs` 에 세 곳 교차 검사를 추가했다. `build.mjs` 는 파일명을 하드코딩하지 않고
`src/assets/*.png` 를 전부 복사하도록 바꿔, 캐릭터 추가 시 빌드 스크립트는 손댈 필요가 없게 했다.

**게이트**: `npm run check` 그린. 유닛 85→93(characters 8), E2E 12→13(`character.spec.ts`).
E2E 는 사이드패널 썸네일 클릭 → storage 저장 → 열려 있던 오버레이 교체 → 오염된 id 폴백까지 전 경로를 본다.

### 결정 18 — pet2 스프라이트를 고해상도 원본으로 재생성

결정 17 에서 `pet2.png` 를 이미 축소된 576×104 시트 그대로 썼더니 캐릭터가 `pet` 의 절반 크기(높이 45px)로 나왔다.
사용자가 "너무 작다" 고 판단해 고해상도 원본(`pet2_large.png`, 1152×104, 알파 포함)을 제공했다.

**왜 업스케일로 해결하지 않았나**: 기존 576×104 시트를 키우는 방안을 먼저 계산했는데,
모든 프레임에 적용 가능한 **균일 배율이 1.05배**에 불과했다 — `sleep` 프레임이 이미 폭 59px 라 병목이었다.
프레임별로 다른 배율을 주면 프레임 간 캐릭터 크기가 튄다. 즉 원본 없이는 해결 불가였고, 원본을 받아 재생성하는 게 정답이었다.

**mirrorX 옵션 추가**: `makeSprite` 가 모듈 상수 `MIRROR_X` 를 쓰고 있어 캐릭터별로 다른 방향을 줄 수 없었다.
세 번째 인자 `{ mirrorX }` 로 뺐고 기본값은 기존(true)이라 `pet` 동작은 그대로다.
`pet2` 소스는 이미 오른쪽을 보고 있어 **false**. 판별은 추측하지 않고 두 버전을 생성해
`pet.png` 의 walk 프레임과 3배 확대로 나란히 놓고 눈으로 확인했다(코가 향하는 방향).

**SHEETS 배열**: 스크립트 하단이 단일 `SRC`/`DEST` 하드코딩이라 캐릭터가 늘면 매번 고쳐야 했다.
`SHEETS` 배열로 바꿔 두 캐릭터를 한 번에 재생성한다. 캐릭터 추가 시 한 줄만 넣으면 된다.

**결과 크기**: 서 있는 프레임 높이 45px → **79~89px**. `pet`(100px)보다는 여전히 짧은데,
make-sprite 가 주 캐릭터를 `TARGET_W=58`·`TARGET_H=100` 안에 맞추기 때문이다.
`pet2` 는 체형이 통통해 **폭이 먼저 58 에 차서** 높이가 100 에 못 간다. 아트 자체의 비율이라 정상 동작이며,
더 키우려면 `TARGET_W` 를 올려야 하는데 그러면 셀(64px)을 넘어 잘린다.

**주의(삽질 기록)**: `make-sprite.mjs` 는 top-level 에서 즉시 실행되므로 `import` 만 해도 시트가 재생성된다.
후보 생성용으로 import 했다가 `pet.png` 가 덮어써졌다 — 결과가 바이트 동일해 사고는 없었지만,
이 스크립트를 모듈로 재사용할 땐 실행 부작용을 염두에 둬야 한다.

**게이트**: `npm run check` 그린 — 유닛 93 · E2E 13 그대로(스프라이트 교체는 코드 변경이 아니라 테스트 수 불변).

### 결정 19 — 놀아주기(클릭·드롭 → 행복도) + 행복도에 따른 웃음 빈도

사용자 요청: "떨구거나 클릭하면 놀아주기로 행복도를 올리고, 행복도가 높으면 더 자주 웃도록."

**불변식 변경(의도적)**: 결정 10 의 "행복도를 바꾸는 경로는 decay·feed 둘뿐" 이 셋이 된다.
결정 15(대화 성장)가 같은 이유로 롤백된 전례가 있어 신중히 봤는데 성격이 다르다.
낮잠처럼 **연출이 몰래 상태를 만지는 게 아니라** 사용자의 명시적 상호작용에 대한 보상이고,
`play` 는 **행복도만** 건드린다(배고픔·`lastUpdated` 불변). 배고픔을 바꾸는 경로는 여전히 decay·feed 둘뿐이다.

**클릭과 드롭을 구분하지 않았다**: 클릭도 pointerdown→pointerup 이라 이미 `endDrag` 를 거친다.
이동 거리로 클릭/드래그를 가르는 코드를 넣을 이유가 없어 `endDrag` 한 곳에만 배선했다.

**쿨다운이 두 겹인 이유 — E2E 가 잡은 실제 버그**:
처음엔 core 의 `play(state, now)` 가 `lastPlayedAt`(storage)만 보고 3초 쿨다운을 걸었다.
E2E 에서 연타 3회를 넣었더니 58 이 아니라 **82** 가 나왔다(+8 이 세 번 다 먹힘).
원인은 read-modify-write 경합이다 — 클릭 3개가 거의 동시에 `loadPet()` 을 await 하면
셋 다 "아직 아무도 저장하지 않은" 상태를 읽어 전부 쿨다운을 통과한다.
그래서 content 에 **`await` 앞에서 동기적으로 끊는** 메모리 타임스탬프 가드를 추가했다.
storage 쿨다운은 그대로 둔다 — 새로고침해도, 탭이 여러 개여도 유지돼야 하기 때문이다.
같은 탭 연타는 메모리 가드가, 리로드·다중 탭은 storage 가 담당한다.

**시계 역행 방어**: `now - lastPlayedAt` 이 음수면 쿨다운으로 치지 않는다.
그러지 않으면 시계가 앞섰다 되돌아온 뒤 팻이 영영 반응하지 않는다. core·content 양쪽에 같은 규칙을 뒀다.

**웃음 빈도 — `smilePeriod(happiness)`**:
"자주 웃는다" 를 임계값 하나 낮추는 것(90→70)으로 처리하면 여전히 이분법이라 "더 자주" 가 아니다.
행복도를 **주기**로 환산했다 — 90+ 는 멈출 때마다(1), 70+ 는 두 번에 한 번(2), 60+ 는 세 번에 한 번(3), 미만은 0.
`spriteFrame` 이 `body.clock` 을 `WALK_MS+IDLE_MS` 로 나눈 주기 인덱스로 판정해 **결정적**이다(랜덤 없음 → 재현 가능).
단조성(행복도가 오를수록 주기가 짧아지기만 함)도 테스트로 고정해 "더 행복한데 덜 웃는" 구간을 막았다.
기존 우선순위(배고픔 > 낮잠 > 칭얼)는 그대로 위에 둔다.

**60 을 하한으로 잡은 이유**: 기존 테스트에 "행복도 50 이면 idle" 이 있었다.
50 도 웃게 하면 그 테스트가 깨지는데, 스펙 충돌이 아니라 "중간 기분에선 안 웃는다" 는 기존 의도라 존중했다.

**게이트**: `npm run check` 그린 — 유닛 93→107, E2E 13→14(`play.spec.ts`).

### 결정 20 — pet2 스프라이트 교체 (pet2_source_v2)

사용자가 새 아트(`pet2_new.png`, 1152×104)를 제공해 `pet2_source_v2.png` 로 편입하고 `SHEETS` 를 갈아끼웠다.
걷기 프레임이 크게 개선됐다 — walk1 89→91px, walk2 83→**96px** 로 포즈가 뚜렷해졌다.

**mirrorX 재확인**: walk 포즈가 바뀌었으므로 방향을 다시 검증했다. 이전과 동일하게 **false** 가 맞다
(반전/비반전 두 버전을 만들어 `pet.png` walk 프레임과 3배 확대 비교 — 얼굴이 오른쪽을 향하는 쪽 선택).

**flaky 관측**: 교체 후 첫 `npm run check` 에서 `drag.spec` 이 1회 실패했다(드래그 추적 ±8px 단언).
해당 단언은 `pointerup` **이전** 구간이라 이번 변경(드롭 시 놀아주기)과 무관하다.
drag.spec 단독 5회 연속 통과 + 전체 게이트 재실행 14/14 통과로 부하성 flaky 로 판정했다.
HANDOFF 섹션 10 에 이미 기록된 "느린 환경에서 perch/drag 계열 flaky 가능" 사례에 해당한다.

### 결정 21 — pet3(푸들 우주인) 캐릭터 추가 + E2E 캐릭터 수 하드코딩 제거

세 번째 캐릭터를 결정 18 에서 세운 파이프라인 그대로 추가했다.
`pet3_source.png`(1152×104, 알파) → `SHEETS` 한 줄 → `CHARACTERS` 한 줄 → manifest 파일명 하나.

**mirrorX = false**: walk 포즈가 정면에 가까워 방향이 모호했다. 두 가지 근거로 무반전을 골랐다.
① walk1·walk2 를 4배 확대해 보니 얼굴·몸이 오른쪽으로 기울어 있다(코드 기본 = 오른쪽 보기).
② 우주복 엠블럼이 **비대칭**이라 반전하면 좌우가 뒤집힌다 — 되돌릴 이유가 없는 손실이다.

**E2E 하드코딩 제거(하네스 보강)**: `character.spec.ts` 가 버튼 개수를 `toHaveCount(2)` 로 박아둬
캐릭터를 늘릴 때마다 스펙이 깨지는 구조였다. `CHARACTERS.length` 로 바꿔 목록에서 파생시켰다.
`characters.ts` 는 chrome 의존이 없는 순수 모듈이라 Playwright 스펙에서 그대로 import 된다.
이제 캐릭터 추가 시 손댈 곳은 **3곳뿐**이고 빌드·E2E 는 자동으로 따라온다.

**크기**: 서 있는 프레임 67~81px. pet2(79~96px)보다 조금 작은데 체형이 통통해 폭(58)이 먼저 차기 때문이며,
결정 18 에 기록한 것과 같은 정상 동작이다.

**게이트**: `npm run check` 그린 — 유닛 107 · E2E 14(개수 변화 없음, 기존 스펙이 3종을 커버).
